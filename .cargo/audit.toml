# cargo-audit 安全审计配置
# https://docs.rs/cargo-audit/latest/cargo_audit/config/

[advisories]
# RUSTSEC-2025-0119: number_prefix is unmaintained
# 传递依赖（indicatif → number_prefix），无法直接升级
# 仅影响终端列宽计算，不影响安全性
ignore = ["RUSTSEC-2025-0119"]
